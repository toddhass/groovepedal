# GroovePedal — React player (`react-ui`)

Experimental React + TypeScript player. `main` stays the proven HTML player.

This branch is a static build (`index.html` + `assets/`). Vercel should deploy it as Other/static — not production.
